#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [[ -x "$REPO_ROOT/.bin/sequin_tool" ]]; then
  exec "$REPO_ROOT/.bin/sequin_tool" "$@"
fi

exec crystal run "$REPO_ROOT/src/sequin_tool.cr" -- "$@"
